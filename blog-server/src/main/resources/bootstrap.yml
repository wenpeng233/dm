spring:
  application:
    name: blog-server

  cloud:
    config:
      label: master
      uri: http://localhost:7900/
      profile: dev

eureka:
  client:
    service-uri:
      defaultZone: http://admin:admin123@localhost:7776/eureka/